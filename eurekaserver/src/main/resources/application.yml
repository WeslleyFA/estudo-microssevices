spring:
  application:
    name: eurekaserver
  security:
    basic:
      enabled: true
    user:
      name: weslley
      password: w12345

server:
  port: 8761

eureka:
  server:
    wait-time-in-ms-when-sync-empty: 0
  client:
    register-with-eureka: false
    fetch-registry: false
